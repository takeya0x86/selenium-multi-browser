apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    jcenter()
}

dependencies {
    testImplementation group: 'org.seleniumhq.selenium', name: 'selenium-java', version: '3.4.0'
    testImplementation group: 'io.github.bonigarcia', name: 'webdrivermanager', version: '1.7.1'
    testImplementation group: 'junit', name: 'junit', version: '4.12'
    testImplementation group: 'org.hamcrest', name: 'hamcrest-library', version: '1.3'
    testRuntimeOnly group: 'org.slf4j', name: 'slf4j-simple', version: '1.7.25'
}

test {
    testLogging {
        showStandardStreams = true
    }
}

tasks.withType(JavaCompile).each {
    it.options.encoding = 'UTF-8'
}
